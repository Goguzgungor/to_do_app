name: to_do_app
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_mobx: ^2.0.5
  mobx: ^2.0.7
  cupertino_icons: ^1.0.2
  mobx_codegen: ^2.0.5+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.10

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

 